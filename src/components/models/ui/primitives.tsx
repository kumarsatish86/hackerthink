/**
 * Models UI primitives — re-export ht-ui to avoid dual systems.
 * Prefer importing from `@/components/ht-ui` for new code.
 */
export { Button, Card, Badge, Skeleton, Tooltip } from '@/components/ht-ui';
