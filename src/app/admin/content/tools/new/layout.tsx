import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Add New Tool - Admin - LinuxConcept',
};

export default function NewToolLayout({ children }: { children: React.ReactNode }) {
  return children;
} 